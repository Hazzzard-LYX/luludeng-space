const togetherSince=new Date("2026-06-26T20:00:00+08:00");function updateTime(){const t=Math.floor(Math.max(0,Date.now()-togetherSince.getTime())/1000);const v={days:Math.floor(t/86400),hours:Math.floor(t%86400/3600),minutes:Math.floor(t%3600/60),seconds:t%60};Object.entries(v).forEach(([id,n])=>document.getElementById(id).textContent=String(n).padStart(2,"0"))}updateTime();setInterval(updateTime,1000);
